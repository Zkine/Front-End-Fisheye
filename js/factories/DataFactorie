class DataFactorie {
  constructor(data, type) {
    if (type === "photographers") {
      return new PhotographersModel(data);
    } else if (type === "photographersMedia") {
      return new PhotographersMedia(data);
    } else {
      throw "Unknown type format";
    }
  }
}
